spring:
  application:
    name: authentication-service
  profiles:
    default: local
  mvc:
    problemdetails:
      enabled: true
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

security:
  jwt:
    access-token-ttl: ${JWT_ACCESS_TOKEN_TTL:PT10M}

server:
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
